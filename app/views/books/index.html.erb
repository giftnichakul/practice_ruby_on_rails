<h1>List of books 📚</h1>

<ul>
  <% @books.each do |book| %>
    <li>
      <a href="<%= book_path(book) %>"><%= book.name%></a>
      <%= book.reviews.any? ?
        + book.reviews.average('star').to_s + ' stars'
      : 'no reviews' %>
    </li>
  <%end%>
</ul>
